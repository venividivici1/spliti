__pycache__/
*.py[cod]

# virtualenvs
.venv
venv/

# secrets
.env
.envrc

# local SQLite database (keep split.db out of version control)
spliti/*.db

# test / coverage artifacts
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
vapid_private.pem

# Google Cloud service-account keys (Firestore mirror credentials)
*-service-account.json
service-account*.json
gcp-credentials*.json
gcp_sa.json

# WSL/Windows download artifacts
*:Zone.Identifier
